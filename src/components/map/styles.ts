import MapView, {MapViewProps} from 'react-native-maps';
import styled from 'styled-components/native';

export const StyledMapView = styled<MapViewProps>(MapView)`
  width: 100%;
  height: 100%;
`;
