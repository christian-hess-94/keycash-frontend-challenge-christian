import {Button, Text, View} from 'react-native';
import styled from 'styled-components/native';

export const PaginationHeaderContainer = styled(View)`
  flex-direction: row;
  justify-content: space-around;
`;
export const PaginationHeaderButton = styled(Button)``;
export const PaginationHeaderText = styled(Text)`
  color: darkslategray;
`;
